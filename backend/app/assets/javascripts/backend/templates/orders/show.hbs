{{#with this as order}}
  <div id='states'>
    {{#each states}}
      <span id='{{this}}_state' data-state="{{unbound this}}" class='state_indicator'>
        {{link-to this "order.state" order.number this}}
      </span>
    {{/each}}
  </div>
  {{partial 'orders/states' }}

{{/with}}